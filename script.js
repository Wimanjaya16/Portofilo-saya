// Tambahkan interaktivitas dengan JavaScript di sini jika diperlukan
document.querySelector('form').addEventListener('submit', function(event) {
    event.preventDefault();
    alert('Formulir telah dikirim!');
});
